# Monitor Configuration
# ConfigMap with team name and API endpoint

apiVersion: v1
kind: ConfigMap
metadata:
  name: monitor-config
  # TODO: Change namespace to your team name
  namespace: the-hole
  labels:
    app: team-monitor
data:
  TEAM_NAME: "the-hole"
  API_ENDPOINT: "https://chas-academy-devops-2026-2qx27gnw8-erkan-djafers-projects.vercel.app/api/team-status"
  CHECK_INTERVAL: "30000"  # Check every 30 seconds
